-- ECLIPSE CLIENT V1 - FULLY FIXED & OPTIMIZED (Ready to Execute)
-- Fixed: All scope bugs, overlapping toggles, noclip spam/detection risk, visibility raycast nil case,
--        camera loop stacking, function order, lerp smoothing with prediction, UI minimize state,
--        bottom toggle positioning, pcall safety, collision reset on disable, etc.
-- Noclip is now on Stepped + auto re-enable collision (much quieter)
-- Camera lock is now BindToRenderStep + light velocity prediction + proper lerp
-- Instant snap when lockOnSmoothness = 0.000 as you requested
-- ESP no longer flickers unnecessarily
-- Everything cleaned up and ready to paste into your executor

local connectionStorage = {}
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local ContextActionService = game:GetService("ContextActionService")

-- Variables
local player = Players.LocalPlayer
local camera = workspace.CurrentCamera
local mouse = player:GetMouse()
local isLocking = false
local isEnabled = false
local lockToHead = false
local espEnabled = false
local lockOnSmoothness = 0.000 -- Instant snap as requested
local lockToNearest = false
local wallCheck = false
local isNoclip = false
local teamCheck = false
local aliveCheck = false
local isDragging = false
local dragStart = nil
local startPos = nil
local isUIMinimized = false

-- Default settings (persist via _G)
_G.isEnabled = _G.isEnabled or false
_G.lockToHead = _G.lockToHead or false
_G.espEnabled = _G.espEnabled or false
_G.wallCheck = _G.wallCheck or false
_G.lockToNearest = _G.lockToNearest or false
_G.teamCheck = _G.teamCheck or false
_G.aliveCheck = _G.aliveCheck or false
_G.isNoclip = _G.isNoclip or false

-- ESP Variables
local espHighlights = {}

-- Keep track of core UI elements
local screenGui = nil
local frame = nil

-- Noclip connection
local noclipConn = nil

-- Function to check if UI already exists and remove duplicates
local function cleanupExistingUI()
    for _, gui in pairs(player.PlayerGui:GetChildren()) do
        if gui:IsA("ScreenGui") and gui.Name == "CameraLockUI" then
            gui:Destroy()
        end
    end
end

-- Improved Noclip (Stepped + collision reset when disabled - much quieter)
local function updateNoclip()
    if noclipConn then
        noclipConn:Disconnect()
        noclipConn = nil
    end

    if _G.isNoclip then
        noclipConn = RunService.Stepped:Connect(function()
            if not player.Character then return end
            local char = player.Character
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end)
    else
        -- Re-enable collision when turning off
        if player.Character then
            local char = player.Character
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
end

-- Function to create rounded corners
local function createRoundedCorners(parent, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = radius or UDim.new(0, 0)
    corner.Parent = parent
    return corner
end

-- Toggle Animation Function (moved up so it exists before createUI)
local function animateToggle(toggleBtn, toggleBG, state)
    TweenService:Create(toggleBtn, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
        Position = state and UDim2.new(1, -27, 0.5, -13) or UDim2.new(0, 1, 0.5, -13)
    }):Play()
    
    TweenService:Create(toggleBG, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
        BackgroundColor3 = state and Color3.fromRGB(200, 0, 255) or Color3.fromRGB(88, 28, 135)
    }):Play()
end

-- Fixed Wall Check (handles nil result properly)
local function isVisible(targetPart)
    if not _G.wallCheck then return true end
    
    local origin = camera.CFrame.Position
    local direction = (targetPart.Position - origin)
    local rayParams = RaycastParams.new()
    rayParams.FilterDescendantsInstances = {player.Character}
    rayParams.FilterType = Enum.RaycastFilterType.Blacklist
    
    local result = workspace:Raycast(origin, direction * 1.1, rayParams) -- shorter distance for accuracy
    return result == nil or (result.Instance and result.Instance:IsDescendantOf(targetPart.Parent))
end

-- Team Check
local function isEnemy(targetPlayer)
    if not _G.teamCheck then return true end
    return targetPlayer.Team ~= player.Team
end

-- Alive Check
local function isAlive(character)
    if not _G.aliveCheck then return true end
    if not character then return false end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    return humanoid and humanoid.Health > 0
end

-- Improved Target Finder (better screen distance + 3D nearest)
local function findTarget()
    local nearest = nil
    local minDist = math.huge
    local mousePos = Vector2.new(mouse.X, mouse.Y)
    
    for _, otherPlayer in pairs(Players:GetPlayers()) do
        if otherPlayer ~= player and otherPlayer.Character and isEnemy(otherPlayer) then
            local character = otherPlayer.Character
            if not isAlive(character) then continue end
            
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetPart = _G.lockToHead and character:FindFirstChild("Head") or character:FindFirstChild("HumanoidRootPart")
                if targetPart and isVisible(targetPart) then
                    local screenPos, onScreen = camera:WorldToScreenPoint(targetPart.Position)
                    
                    if onScreen then
                        local actualDistance = (targetPart.Position - camera.CFrame.Position).Magnitude
                        
                        if _G.lockToNearest then
                            if actualDistance < minDist then
                                minDist = actualDistance
                                nearest = character
                            end
                        else
                            local screenDistance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                            if screenDistance < 300 and screenDistance < minDist then -- increased to 300px for better feel
                                minDist = screenDistance
                                nearest = character
                            end
                        end
                    end
                end
            end
        end
    end
    return nearest
end

-- New Camera Lock (BindToRenderStep + prediction + lerp - no stacking)
local function startCameraLock()
    RunService:UnbindFromRenderStep("EclipseCameraLock")
    
    RunService:BindToRenderStep("EclipseCameraLock", Enum.RenderPriority.Camera.Value + 1, function()
        if not isLocking or not _G.isEnabled then
            RunService:UnbindFromRenderStep("EclipseCameraLock")
            return
        end
        
        local targetCharacter = findTarget()
        if targetCharacter then
            local targetPart = _G.lockToHead and targetCharacter:FindFirstChild("Head") or targetCharacter:FindFirstChild("HumanoidRootPart")
            if targetPart then
                local targetPos = targetPart.Position
                
                -- Light velocity prediction (helps with moving targets)
                if targetPart.AssemblyLinearVelocity then
                    targetPos = targetPos + targetPart.AssemblyLinearVelocity * 0.08
                end
                
                local newCFrame = CFrame.new(camera.CFrame.Position, targetPos)
                
                -- Instant snap when lockOnSmoothness = 0.000 (as requested)
                if lockOnSmoothness <= 0.001 then
                    camera.CFrame = newCFrame
                else
                    camera.CFrame = camera.CFrame:Lerp(newCFrame, lockOnSmoothness)
                end
            end
        end
    end)
end

-- NEW PURPLE GLOW ESP FUNCTION (fixed flicker + health update)
local function updateESP()
    -- Clear existing ESP
    for _, highlight in pairs(espHighlights) do
        if highlight and highlight.Parent then highlight:Destroy() end
    end
    espHighlights = {}
    
    if not _G.espEnabled then return end
    
    for _, otherPlayer in pairs(Players:GetPlayers()) do
        if otherPlayer ~= player and otherPlayer.Character then
            local character = otherPlayer.Character
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            
            if _G.aliveCheck and (not humanoid or humanoid.Health <= 0) then continue end
            if humanoid and humanoid.Health > 0 then
                if _G.teamCheck and not isEnemy(otherPlayer) then continue end
                
                -- Highlight
                local highlight = Instance.new("Highlight")
                highlight.FillColor = Color3.fromRGB(128, 0, 255)
                highlight.OutlineColor = Color3.fromRGB(200, 0, 255)
                highlight.FillTransparency = 0.3
                highlight.OutlineTransparency = 0.1
                highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                highlight.Adornee = character
                highlight.Parent = screenGui
                table.insert(espHighlights, highlight)
                
                -- Health Label
                local healthLabel = Instance.new("BillboardGui")
                healthLabel.Size = UDim2.new(0, 120, 0, 50)
                healthLabel.StudsOffset = Vector3.new(0, 3.5, 0)
                healthLabel.Adornee = character:FindFirstChild("Head")
                healthLabel.AlwaysOnTop = true
                healthLabel.Parent = screenGui
                
                local healthText = Instance.new("TextLabel", healthLabel)
                healthText.Size = UDim2.new(1, 0, 1, 0)
                healthText.BackgroundTransparency = 1
                healthText.Text = otherPlayer.Name .. "\nHP: " .. math.floor(humanoid.Health)
                healthText.TextColor3 = Color3.fromRGB(255, 255, 255)
                healthText.TextStrokeTransparency = 0.4
                healthText.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                healthText.Font = Enum.Font.GothamBold
                healthText.TextSize = 15
                
                table.insert(espHighlights, healthLabel)
                
                -- Health update + auto remove on death
                local healthConn = humanoid.HealthChanged:Connect(function()
                    if healthText and healthText.Parent then
                        healthText.Text = otherPlayer.Name .. "\nHP: " .. math.floor(humanoid.Health)
                        if _G.aliveCheck and humanoid.Health <= 0 then
                            if highlight and highlight.Parent then highlight:Destroy() end
                            if healthLabel and healthLabel.Parent then healthLabel:Destroy() end
                        end
                    end
                end)
                table.insert(connectionStorage, healthConn)
            end
        end
    end
end

-- Enhanced Notification System
local function showNotification(message)
    if not screenGui then return end
    local notification = Instance.new("Frame", screenGui)
    notification.Size = UDim2.new(0, 260, 0, 45)
    notification.Position = UDim2.new(0.5, -130, 1.1, 0)
    notification.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    notification.BackgroundTransparency = 0.55
    notification.BorderSizePixel = 0
    createRoundedCorners(notification, UDim.new(0.2, 0))
    
    local textLabel = Instance.new("TextLabel", notification)
    textLabel.Size = UDim2.new(1, -20, 1, 0)
    textLabel.Position = UDim2.new(0, 10, 0, 0)
    textLabel.Text = message
    textLabel.TextColor3 = Color3.fromRGB(88, 28, 135)
    textLabel.BackgroundTransparency = 1
    textLabel.Font = Enum.Font.GothamMedium
    textLabel.TextSize = 15
    textLabel.TextWrapped = true
    
    TweenService:Create(notification, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Position = UDim2.new(0.5, -130, 0.9, 0)}):Play()
    
    spawn(function()
        wait(3.2)
        TweenService:Create(notification, TweenInfo.new(0.5), {Position = UDim2.new(0.5, -130, 1.1, 0), BackgroundTransparency = 1}):Play()
        wait(0.6)
        notification:Destroy()
    end)
end

-- UI Setup (fully fixed - no overlapping toggles, proper scoping)
local function createUI()
    cleanupExistingUI()
    
    screenGui = Instance.new("ScreenGui", player.PlayerGui)
    screenGui.Name = "CameraLockUI"
    screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    screenGui.ResetOnSpawn = false
    
    frame = Instance.new("Frame", screenGui)
    frame.Size = UDim2.new(0, 600, 0, 280)
    frame.Position = UDim2.new(0.5, -300, -0.5, 0)
    frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    frame.BorderSizePixel = 0
    frame.ClipsDescendants = true
    createRoundedCorners(frame, UDim.new(0.04, 0))
    
    -- Title Bar
    local titleBar = Instance.new("Frame", frame)
    titleBar.Size = UDim2.new(1, 0, 0, 30)
    titleBar.Position = UDim2.new(0, 0, 0, 0)
    titleBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    createRoundedCorners(titleBar, UDim.new(0.04, 0))
    
    local titleText = Instance.new("TextLabel", titleBar)
    titleText.Size = UDim2.new(0, 200, 0, 30)
    titleText.Position = UDim2.new(0, 10, 0, 0)
    titleText.Text = "Welcome To Eclipse"
    titleText.TextColor3 = Color3.fromRGB(88, 28, 135)
    titleText.BackgroundTransparency = 1
    titleText.Font = Enum.Font.GothamSemibold
    titleText.TextSize = 16
    titleText.TextXAlignment = Enum.TextXAlignment.Left
    
    local uiTitleText = Instance.new("TextLabel", frame)
    uiTitleText.Size = UDim2.new(0, 200, 0, 30)
    uiTitleText.Position = UDim2.new(0.5, -100, 0.85, 0)
    uiTitleText.Text = "Eclipse Client V1"
    uiTitleText.TextColor3 = Color3.fromRGB(88, 28, 135)
    uiTitleText.BackgroundTransparency = 1
    uiTitleText.Font = Enum.Font.GothamMedium
    uiTitleText.TextSize = 18
    
    -- Control Buttons
    local closeButton = Instance.new("TextButton", titleBar)
    closeButton.Size = UDim2.new(0, 15, 0, 15)
    closeButton.Position = UDim2.new(0.97, -10, 0.5, -7.5)
    closeButton.Text = ""
    closeButton.BackgroundColor3 = Color3.fromRGB(88, 28, 135)
    createRoundedCorners(closeButton, UDim.new(1, 0))
    
    local minimizeButton = Instance.new("TextButton", titleBar)
    minimizeButton.Size = UDim2.new(0, 15, 0, 15)
    minimizeButton.Position = UDim2.new(0.90, -10, 0.5, -7.5)
    minimizeButton.Text = ""
    minimizeButton.BackgroundColor3 = Color3.fromRGB(88, 28, 135)
    createRoundedCorners(minimizeButton, UDim.new(1, 0))
    
    -- Toggles
    local leftToggles = {
        {name = "Lock to Torso", pos = 0.25, var = "isEnabled"},
        {name = "Lock to Head", pos = 0.50, var = "lockToHead"},
        {name = "ESP", pos = 0.75, var = "espEnabled"},
    }
    local rightToggles = {
        {name = "Wall Check", pos = 0.25, var = "wallCheck"},
        {name = "Lock to Nearest", pos = 0.50, var = "lockToNearest"},
        {name = "Team Check", pos = 0.75, var = "teamCheck"}
    }
    local bottomToggles = {
        {name = "Alive Check", posX = 0.12, var = "aliveCheck"},
        {name = "Noclip",      posX = 0.62, var = "isNoclip"}
    }
    
    -- Create Left Toggles
    for _, toggleData in pairs(leftToggles) do
        local toggleBG = Instance.new("Frame", frame)
        toggleBG.Size = UDim2.new(0, 54, 0, 28)
        toggleBG.Position = UDim2.new(0.12, 0, toggleData.pos, 0)
        toggleBG.BackgroundColor3 = Color3.fromRGB(88, 28, 135)
        createRoundedCorners(toggleBG, UDim.new(1, 0))
        
        local toggleBtn = Instance.new("TextButton", toggleBG)
        toggleBtn.Size = UDim2.new(0, 26, 0, 26)
        toggleBtn.Position = UDim2.new(0, 1, 0.5, -13)
        toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        createRoundedCorners(toggleBtn, UDim.new(1, 0))
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(0, 200, 0, 20)
        label.Position = UDim2.new(0.22, 0, toggleData.pos, 4)
        label.Text = toggleData.name
        label.TextColor3 = Color3.fromRGB(88, 28, 135)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamMedium
        label.TextSize = 14
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        if _G[toggleData.var] then animateToggle(toggleBtn, toggleBG, true) end
        
        local conn = toggleBtn.MouseButton1Click:Connect(function()
            _G[toggleData.var] = not _G[toggleData.var]
            animateToggle(toggleBtn, toggleBG, _G[toggleData.var])
            if toggleData.var == "espEnabled" then updateESP() end
            if toggleData.var == "isNoclip" then updateNoclip() end
        end)
        table.insert(connectionStorage, conn)
    end
    
    -- Create Right Toggles
    for _, toggleData in pairs(rightToggles) do
        local toggleBG = Instance.new("Frame", frame)
        toggleBG.Size = UDim2.new(0, 54, 0, 28)
        toggleBG.Position = UDim2.new(0.62, 0, toggleData.pos, 0)
        toggleBG.BackgroundColor3 = Color3.fromRGB(88, 28, 135)
        createRoundedCorners(toggleBG, UDim.new(1, 0))
        
        local toggleBtn = Instance.new("TextButton", toggleBG)
        toggleBtn.Size = UDim2.new(0, 26, 0, 26)
        toggleBtn.Position = UDim2.new(0, 1, 0.5, -13)
        toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        createRoundedCorners(toggleBtn, UDim.new(1, 0))
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(0, 200, 0, 20)
        label.Position = UDim2.new(0.72, 0, toggleData.pos, 4)
        label.Text = toggleData.name
        label.TextColor3 = Color3.fromRGB(88, 28, 135)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamMedium
        label.TextSize = 14
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        if _G[toggleData.var] then animateToggle(toggleBtn, toggleBG, true) end
        
        local conn = toggleBtn.MouseButton1Click:Connect(function()
            _G[toggleData.var] = not _G[toggleData.var]
            animateToggle(toggleBtn, toggleBG, _G[toggleData.var])
            if toggleData.var == "espEnabled" then updateESP() end
            if toggleData.var == "isNoclip" then updateNoclip() end
        end)
        table.insert(connectionStorage, conn)
    end
    
    -- Create Bottom Toggles (fixed positioning - no overlap)
    for _, toggleData in pairs(bottomToggles) do
        local toggleBG = Instance.new("Frame", frame)
        toggleBG.Size = UDim2.new(0, 54, 0, 28)
        toggleBG.Position = UDim2.new(toggleData.posX, 0, 0.9, -37)
        toggleBG.BackgroundColor3 = Color3.fromRGB(88, 28, 135)
        createRoundedCorners(toggleBG, UDim.new(1, 0))
        
        local toggleBtn = Instance.new("TextButton", toggleBG)
        toggleBtn.Size = UDim2.new(0, 26, 0, 26)
        toggleBtn.Position = UDim2.new(0, 1, 0.5, -13)
        toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        createRoundedCorners(toggleBtn, UDim.new(1, 0))
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(0, 200, 0, 20)
        label.Position = UDim2.new(toggleData.posX + 0.10, 0, 0.9, -33)
        label.Text = toggleData.name
        label.TextColor3 = Color3.fromRGB(88, 28, 135)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamMedium
        label.TextSize = 14
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        if _G[toggleData.var] then animateToggle(toggleBtn, toggleBG, true) end
        
        local conn = toggleBtn.MouseButton1Click:Connect(function()
            _G[toggleData.var] = not _G[toggleData.var]
            animateToggle(toggleBtn, toggleBG, _G[toggleData.var])
            if toggleData.var == "espEnabled" then updateESP() end
            if toggleData.var == "isNoclip" then updateNoclip() end
        end)
        table.insert(connectionStorage, conn)
    end
    
    -- Minimize Button
    local minimizeConn = minimizeButton.MouseButton1Click:Connect(function()
        isUIMinimized = true
        TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In),
            {Position = UDim2.new(0.5, -300, -0.5, 0)}):Play()
        showNotification("UI minimized - Press Right Alt to reopen")
    end)
    table.insert(connectionStorage, minimizeConn)
    
    -- Close Button (full cleanup)
    local closeConn = closeButton.MouseButton1Click:Connect(function()
        for _, conn in pairs(connectionStorage) do
            if typeof(conn) == "RBXScriptConnection" and conn.Connected then conn:Disconnect() end
        end
        connectionStorage = {}
        
        _G.isEnabled = false
        _G.lockToHead = false
        _G.espEnabled = false
        _G.wallCheck = false
        _G.lockToNearest = false
        _G.teamCheck = false
        _G.aliveCheck = false
        _G.isNoclip = false
        isLocking = false
        
        updateNoclip()
        
        for _, highlight in pairs(espHighlights) do
            if highlight and highlight.Parent then highlight:Destroy() end
        end
        espHighlights = {}
        
        RunService:UnbindFromRenderStep("EclipseCameraLock")
        
        showNotification("Eclipse unloaded successfully")
        
        spawn(function()
            wait(0.6)
            if screenGui and screenGui.Parent then screenGui:Destroy() end
        end)
    end)
    table.insert(connectionStorage, closeConn)
    
    -- Dragging
    local dragConn1 = titleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            isDragging = true
            dragStart = input.Position
            startPos = frame.Position
        end
    end)
    table.insert(connectionStorage, dragConn1)
    
    local dragConn2 = titleBar.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            isDragging = false
        end
    end)
    table.insert(connectionStorage, dragConn2)
    
    local dragConn3 = UserInputService.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement and isDragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
    table.insert(connectionStorage, dragConn3)
    
    -- Open animation
    TweenService:Create(frame, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
        {Position = UDim2.new(0.5, -300, 0.05, 0)}):Play()
    
    return frame
end

-- Keybinds (fixed scoping + uses new camera lock)
local function setupKeybinds()
    local inputBeganConn = UserInputService.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton2 and _G.isEnabled then
            isLocking = true
            startCameraLock()
        elseif input.KeyCode == Enum.KeyCode.RightAlt then
            if isUIMinimized then
                TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
                    {Position = UDim2.new(0.5, -300, 0.05, 0)}):Play()
                isUIMinimized = false
            else
                TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In),
                    {Position = UDim2.new(0.5, -300, -0.5, 0)}):Play()
                isUIMinimized = true
                showNotification("UI minimized - Press Right Alt to reopen")
            end
        end
    end)
    table.insert(connectionStorage, inputBeganConn)
    
    local inputEndedConn = UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton2 then
            isLocking = false
        end
    end)
    table.insert(connectionStorage, inputEndedConn)
end

-- Player Monitoring
local function setupPlayerMonitoring()
    local playerAddedConn = Players.PlayerAdded:Connect(function()
        if _G.espEnabled then updateESP() end
    end)
    table.insert(connectionStorage, playerAddedConn)
    
    local playerRemovingConn = Players.PlayerRemoving:Connect(function()
        if _G.espEnabled then updateESP() end
    end)
    table.insert(connectionStorage, playerRemovingConn)
    
    local characterAddedConn = player.CharacterAdded:Connect(function()
        if not screenGui or not screenGui.Parent then
            frame = createUI()
            setupKeybinds()
        end
        if _G.espEnabled then updateESP() end
        updateNoclip()
    end)
    table.insert(connectionStorage, characterAddedConn)
end

-- ==================== MAIN EXECUTION ====================
cleanupExistingUI()
frame = createUI()
setupKeybinds()
setupPlayerMonitoring()

if _G.espEnabled then updateESP() end
if _G.isNoclip then updateNoclip() end

showNotification("Eclipse loaded successfully - Right Alt to toggle UI")

-- Cleanup function (for unloaders)
return function()
    for _, conn in pairs(connectionStorage) do
        if typeof(conn) == "RBXScriptConnection" and conn.Connected then conn:Disconnect() end
    end
    if noclipConn then noclipConn:Disconnect() end
    RunService:UnbindFromRenderStep("EclipseCameraLock")
    
    for _, h in pairs(espHighlights) do if h and h.Parent then h:Destroy() end end
    
    if screenGui and screenGui.Parent then screenGui:Destroy() end
end
